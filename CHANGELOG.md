# 坑位图鉴 — 版本日志

版本号遵循语义化版本（SemVer）：`MAJOR.MINOR.PATCH`。
- `MAJOR`：破坏性架构变更
- `MINOR`：新增能力，向后兼容
- `PATCH`：Bug 修复 / 体验优化

---

## [v0.2.0]

> 代码已合入但尚未同步到 `docs/index.html` 对外文档的功能。
> 发布新版本时，把对应条目下迁到新的版本号小节。

### 🏢 新增：公司图鉴

独立的「公司图鉴」Tab，**按公司维度**把散落的岗位聚合成一张一张的公司卡片，支持多种浏览 / 筛选 / 对比维度：

**图鉴总览**
- **进度与想去清单**：顶部实时显示「已鉴定 X / 总公司 Y 家」；想去收藏可一键切换「只看想去 / 全部」
- **三套即时榜单切换**：
  - 🏆 分数榜：公司分从高到低
  - 🔥 招聘力度榜：在招岗位数从多到少
  - 💰 薪资榜：平均月薪从高到低
- **多维筛选**：公司名搜索 + 行业（带公司数）+ 融资阶段（带公司数）

**三种浏览模式**

| 模式 | 能看到什么 |
| --- | --- |
| **卡片墙**（默认） | 每家公司一张卡片：排名徽章 / 公司分 / 在招岗数 / 行业·融资·规模 / 均薪·城市 / 最佳岗位（皇冠+分数）；想去收藏 / 加入对比 / 「去鉴定」解锁未开卡公司 |
| **象限气泡图** | X=平均薪资、Y=公司分、气泡大小=在招岗位数；右上角标注「高分·高薪甜蜜点」；薪资未知的公司单独列成 chip，点击气泡直接进公司 |
| **并排对比**（2-4 家） | 每家一列横向对比：公司分 + **四维雷达图**（财务/成长/资源/WLB）+ 关键指标表（规模/阶段/均薪/在招/最佳岗位） |

**公司详情抽屉**（点击卡片/气泡进入）
- **基础信息 chips**：行业、融资阶段、规模、公司性质、成立时间、日均工时、在招岗数、均薪
- **公司简介 + 经营范围**（可展开全文）
- **四维画像雷达**：基于名下所有岗位的规则分均值计算（财务/成长/资源/WLB）
- **AI 评价 · 图鉴词条**：手动触发大模型基于公司资料和在招岗位生成公司专属词条，包含：业务解读、技术栈画像、招聘紧迫度（带理由）、值不值得去（带理由）、亮点 chips、顾虑 chips、面试策略 chips、业务关键词 cloud；历史评价 append-only 保存，可回看、可单条删除
- **名下岗位列表**：直接列该公司所有采到过的岗位（薪资/城市/规则状态分/AI 次数/是否下线），点击直接跳对应岗位详情

**「去鉴定」解锁机制**
- 新公司第一次出现在图鉴里是「锁卡」状态：公司分显示 `?`、详情隐藏
- 点卡片上的「去鉴定」，系统自动挑出公司当前最佳岗位发起 AI 打分；打分完成即自动解锁这家公司，公司分、雷达、最佳岗位等信息一次性全部透出
- 公司打分自带 180 天保鲜缓存，不会频繁重评

**不会无故缺分**
- 用户主动用 AI 评过的岗位，即使被规则层淘汰，也会参与公司平均分的加权计算
- 名下岗位全部被规则淘汰时，回退到公司级 AI 评价分，保证至少有分数参考（之前像雅迪这类公司会显示"无分"，现在不会了）

### 🕷️ 采集更聪明，不会漏也不会白跑

- **续翻接力**：前几页翻不出新职位时，会自动跳到上一次采集停止的页码再往后翻几页；兼顾"前几页优先抓最新职位"和"后面旧职位不会一直采不到"
- **翻到底自动清零**：搜索结果真的翻到最后一页时，续翻锚点自动失效，下次从头开始；不会因为中途被迫中断而误清除接力页码
- **单次采集有上限**：AI 自动采集时默认最多新抓 60 个职位就停，执行时长得预期
- **近期职位不重复抓**：60 天内已采集过的职位直接跳过，节省翻页时间

### 🧠 AI 打分更省、更稳

- **公司打分有 180 天缓存**：公司情况变化慢，大半年内打过的公司分直接复用，不浪费模型调用
- **岗位打分有 90 天保鲜期**：过期自动排队重评，不会一直用过期的评分

---

## [v0.1.0] — 初始发布版

> 对应 `docs/index.html` 官网落地页当前介绍的全部能力。
> 定位：可用的 MVP 主循环（采集 → 打分 → 分析 → 优化 → 校准）已跑通。

### 🎯 核心价值

- **坑位图鉴**：个人猎头系统，本地运行 · 零词元消耗 · Agent Loop 自我进化
- Hero 数据：`4` 评分维度 · `10` 硬性规则 · `4+` AI 大模型 · `0` 词元消耗

### 🔁 Agent Loop（每日循环，`daily` 命令一键编排）

1. **采集** — CDP 驱动 Chrome 增量浏览 BOSS 直聘，连续 3 页全重复即判定已覆盖
2. **打分** — 四维十项规则引擎即时打分，硬淘汰按置信度分级，模糊区间调 AI 介入
3. **分析** — 网页版大模型（DeepSeek / 豆包 / 通义 / Kimi）深度分析，0-10 分 + 面试建议
4. **优化** — 目标岗位 ID + 主简历，生成定制简历（optimize 微调 / rewrite 重写，实验性）
5. **校准** — 打分数据反推画像权重、薪资预期、硬性底线，5 套预设一键切换
6. **迭代** — 更新画像与规则，下一轮更精准

### 💎 四大核心能力（Features）

| 能力 | 要点 |
| --- | --- |
| 自动采集 BOSS 直聘 | CDP 调试模式驱动真实 Chrome；自然访问节奏；连续整页重复降速；覆盖率 ≥90% 放慢基础节奏 |
| 双层打分引擎 | 规则层：4 维度 × 10 项，权重画像驱动；硬淘汰 10 条，置信度 < 60% 标 REVIEW 不误杀；AI 层：10 条触发器自动调用网页大模型 |
| 针对性简历优化（实验性） | 读取 JD 全文 + 主简历，optimize / rewrite 两种风格，JD 关键词融入真实经历，不造假 |
| 预期校准与反馈 | 5 套权重预设（平衡 / 成长 / 财务 / 生活 / 资源优先），数据反向指导调整策略 |

### 📊 打分体系（Scoring System）

**维度 × 权重**
- 财务回报 Finance × 30%（薪资下限/中位数/上限惊喜/股权激励/高价值福利/构成质量）
- 职业发展 Growth × 30%（技术栈重合/公司阶段/业务方向/团队规模/经验学历/技术深度/前瞻性）
- 平台与文化 Resource × 10%（城市匹配/行业重叠/体量偏好/特殊资源/文化适配）
- 工作生活平衡 WLB × 30%（工作模式/弹性福利/企业规范/加班强度/通勤便利）

**硬性淘汰（10 条，FATAL / REVIEW 两级）**

| 编号 | 规则 | 等级 |
| --- | --- | --- |
| H-01 | 城市不可接受 | FATAL |
| H-02 | 行业被拒绝 | FATAL |
| H-03 | 公司规模过小 | FATAL |
| H-04 | 薪资低于硬性下限 | FATAL |
| H-05 | 出差强度超标 | REVIEW |
| H-06 | 加班强度超标（996/大小周） | REVIEW |
| H-07 | 命中排除关键词黑名单 | FATAL |
| H-08 | 外包 / 驻场岗位 | REVIEW |
| H-09 | 学历硬性不符 | FATAL |
| H-10 | 经验年限硬性不符 | REVIEW |

**AI 介入触发器（10 条）**
WLB 偏低偏高需核实 · 行业语义相关需判断 · 总分 6.0-7.5 模糊区间 · JD 内容过短 · 硬规则命中证据不足 · 匿名雇主推测 · 文化适配存疑 · 公司稳定性风险 · 用户主动深度分析（共 10 类场景）

### 👥 两种使用方式（Dual Mode）

| 维度 | Skill + AI 驱动（For Everyone） | CLI + Web 图鉴（For Developers） |
| --- | --- | --- |
| 上手门槛 | 4 步：安装 skill → 复制 BOSS 筛选 URL → 自然语言发指令 → AI 全自动 | 需 Python，`python3 -m gaj <cmd>` 全可控 |
| 交互形态 | 跟 AI 说句话："帮我看看今天有什么新职位" | 命令行 + 浏览器访问 `127.0.0.1:8765` |
| 词元消耗 | 消耗 AI 助手词元 | 零词元消耗（网页大模型免费额度） |
| 定制能力 | AI 自主编排 | 代码完全开放，前后端热重载，任意 AI Agent 可自定义页面 |

### ⌨️ CLI 命令（6 个核心）

| 命令 | 场景 | 典型用法 |
| --- | --- | --- |
| `crawl` | 从 BOSS 列表页 URL 采集 | `gaj crawl "https://zhipin.com/..." [--max-pages N]` |
| `score` | 规则批量打分 | `gaj score --all` / `gaj score --job JOB_ID` |
| `ai-score` | 网页版大模型深度分析 | `gaj ai-score --job JOB_ID --provider deepseek` |
| `resume` | 定制岗位简历（实验性） | `gaj resume --job JOB_ID [--style optimize\|rewrite]` |
| `web` | 启动 Web 图鉴（含热重载） | `gaj web [--port 8765] [--no-reload]` |
| `setup-chrome` / `check` | CDP 环境准备与健康检查 | `gaj setup-chrome` / `gaj check` |

### 🔌 Agent JSON 接口

所有命令走 `python3 -m gaj agent <subcmd>`，stdout 严格只输出一个 JSON 信封，`exit_code` 决策：

| 子命令 | 用途 |
| --- | --- |
| `status` | 健康检查 + 数据概况（任何流程第一步） |
| `jobs` | 查询职位列表（筛选 / 排序 / 分页） |
| `job` | 单职位详情（JD 全文 + 规则分 + AI 分） |
| `analyze` | AI 打分（`--job` 单个 / `--auto` 批量挑候选） |
| `crawl` | 增量采集（自动降速、覆盖提前结束） |
| `daily` | 每日编排：采集 → 挑候选 → AI 分析 → Markdown 摘要 |

**容错保障**：最外层异常兜底，任何失败都返回 JSON，不会静默挂起；数据逐个职位增量落盘，超时 kill 后重跑不重复。

### 🏗️ 技术架构

纯 Python 实现，CDP 驱动真实 Chrome，全链路本地存储，零云端依赖：

```
Chrome CDP (9222)
  → BOSS 爬虫 (boss_scraper)
    → 规则引擎 (gaj/core)
      → AI 分析 (gaj/ai, 4+ 网页版大模型)
        → 本地存储 (data/, 已 gitignore)
          → Web 图鉴 (FastAPI + Alpine.js)
```

**架构要点**
- 数据隐私：所有数据 `data/` 下本地落盘，gitignore 排除
- 多模型支持：DeepSeek / 豆包 / 通义千问 / Kimi，网页版驱动不消耗 API Key
- 自然节奏浏览：连续重复页拉长翻页间隔，覆盖率高时放慢基础节奏
- 代码开放 · 热重载：前后端支持 hot reload，可用任意 AI Agent 自定义
